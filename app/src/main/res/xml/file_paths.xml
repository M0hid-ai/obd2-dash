<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      Exported trip reports only. Scoped to one subdirectory of the cache rather
      than the whole of it, so a share grant can never reach anything else the
      app has cached.
    -->
    <cache-path name="exports" path="exports/" />
</paths>
